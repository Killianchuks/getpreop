"use client";

import { CalendarDays, Clock, Globe2 } from "lucide-react";
import Link from "next/link";
import { useState } from "react";

const slots = [
  "2026-09-03T13:00:00Z", "2026-09-03T16:30:00Z", "2026-09-04T14:00:00Z", "2026-09-04T18:00:00Z", "2026-09-05T15:30:00Z", "2026-09-08T13:30:00Z",
];

export default function PatientAssessmentSchedulingPage() {
  const timezone = Intl.DateTimeFormat().resolvedOptions().timeZone;
  const [selected, setSelected] = useState<string | null>(null);
  const [confirmed, setConfirmed] = useState(false);
  const availableDates = [...new Set(slots.map((slot) => new Intl.DateTimeFormat("en-US", { timeZone: timezone, weekday: "short", month: "short", day: "numeric" }).format(new Date(slot))))];

  return <main className="w-full max-w-none p-0"><section className="mx-auto max-w-5xl px-5 py-10 sm:px-8 lg:px-10"><Link href="/patients/portal" className="text-xs font-semibold text-teal-800 hover:text-teal-950">Back to patient portal</Link><div className="mt-6 grid gap-8 lg:grid-cols-[1fr_0.85fr]"><div><p className="text-xs font-bold uppercase tracking-[0.16em] text-teal-800">Step 2 of 2</p><h1 className="mt-2 text-3xl font-bold tracking-tight text-slate-900">Schedule your anesthesia visit</h1><p className="mt-3 text-sm leading-6 text-slate-600">Your intake is ready for review. Choose a time with your assigned anesthesiologist, Dr. Amara Chen.</p><div className="mt-6 rounded-lg border border-teal-100 bg-teal-50 p-4 text-sm text-teal-950"><p className="flex items-center gap-2 font-semibold"><Globe2 size={17} /> Times shown in your local timezone</p><p className="mt-1 text-xs">{timezone}</p></div><div className="mt-6 space-y-5">{availableDates.map((date) => <section key={date}><h2 className="text-sm font-bold text-slate-900">{date}</h2><div className="mt-3 grid grid-cols-2 gap-3 sm:grid-cols-3">{slots.filter((slot) => new Intl.DateTimeFormat("en-US", { timeZone: timezone, weekday: "short", month: "short", day: "numeric" }).format(new Date(slot)) === date).map((slot) => { const label = new Intl.DateTimeFormat("en-US", { timeZone: timezone, hour: "numeric", minute: "2-digit", timeZoneName: "short" }).format(new Date(slot)); return <button key={slot} type="button" onClick={() => { setSelected(slot); setConfirmed(false); }} className={`rounded-lg border px-3 py-3 text-sm font-semibold transition ${selected === slot ? "border-teal-700 bg-teal-700 text-white" : "border-slate-200 bg-white text-slate-700 hover:border-teal-400"}`}>{label}</button>; })}</div></section>)}</div></div><aside className="h-fit rounded-lg border border-slate-200 bg-white p-6 shadow-sm"><CalendarDays className="text-teal-800" size={25} /><h2 className="mt-5 text-lg font-bold text-slate-900">Your assessment</h2><dl className="mt-5 space-y-4 text-sm"><div><dt className="text-xs font-semibold uppercase tracking-wider text-slate-500">Assigned clinician</dt><dd className="mt-1 font-medium">Dr. Amara Chen</dd></div><div><dt className="text-xs font-semibold uppercase tracking-wider text-slate-500">Visit length</dt><dd className="mt-1 font-medium">30 minutes</dd></div><div><dt className="text-xs font-semibold uppercase tracking-wider text-slate-500">Selected time</dt><dd className="mt-1 font-medium">{selected ? new Intl.DateTimeFormat("en-US", { timeZone: timezone, weekday: "long", month: "long", day: "numeric", hour: "numeric", minute: "2-digit", timeZoneName: "short" }).format(new Date(selected)) : "Choose a time"}</dd></div></dl><button disabled={!selected} onClick={() => setConfirmed(true)} type="button" className="mt-6 w-full rounded-lg bg-teal-800 px-4 py-3 text-sm font-semibold text-white hover:bg-teal-900 disabled:cursor-not-allowed disabled:bg-slate-300">Confirm appointment</button>{confirmed && selected ? <div className="mt-4 rounded-lg bg-teal-50 p-4 text-sm text-teal-900"><p className="font-bold">Your visit is scheduled.</p><p className="mt-1 text-xs">Return at your appointment time to join the secure assessment room.</p><Link href="/patients/portal/assessment/video" className="mt-3 inline-block font-semibold underline">Enter assessment room</Link></div> : null}</aside></div></section></main>;
}
